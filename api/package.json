{
  "name": "bakery-api",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.2"
  },
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning src/server.ts",
    "dev": "node --no-warnings=ExperimentalWarning --watch src/server.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5",
    "vitest": "^3"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  }
}
